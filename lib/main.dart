/*   
1.Create a screen where the user is prompted to enter a location (this could be a city name,
address, or coordinates). textfield ,basic validation ,

2.Include a button that takes the user to the next screen after the location is entered.


3. Implement a feature that allows users to switch between different map types (e.g., satellite, terrain).


4.current location.

*/
import 'package:flutter/material.dart';
import 'package:app/mainscreen.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const MainScreen());
}
